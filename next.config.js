/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/foodburg-host',
    reactStrictMode: true,
    images: {
        unoptimized: true,
    },
};

module.exports = nextConfig;
